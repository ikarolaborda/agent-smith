diff --git a/apparatus/native-clang/fixtures/micro_oob/fuzz_target.cc b/apparatus/native-clang/fixtures/micro_oob/fuzz_target.cc
--- a/apparatus/native-clang/fixtures/micro_oob/fuzz_target.cc
+++ b/apparatus/native-clang/fixtures/micro_oob/fuzz_target.cc
@@ -11,8 +11,8 @@
-  volatile uint8_t *buffer = static_cast<uint8_t *>(std::malloc(8));
+  uint8_t *buffer = static_cast<uint8_t *>(std::malloc(8));
   if (buffer == nullptr) {
     return 0;
   }
-  const size_t attacker_influenced_offset = 8 + (data[4] & 0x0f);
-  buffer[attacker_influenced_offset] = 0x41;
-  std::free(const_cast<uint8_t *>(buffer));
+  const size_t bounded_offset = data[4] & 0x07;
+  buffer[bounded_offset] = 0x41;
+  std::free(buffer);
   return 0;
